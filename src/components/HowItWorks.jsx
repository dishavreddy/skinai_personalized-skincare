import { useScan } from '../context/ScanContext.jsx'
const steps = [['Take a photo','Face a window, skip filters, and choose a clear front-facing image.'],['See your results','SkinAI runs the existing computer-vision analysis on your uploaded pixels.'],['Follow your routine','Review your actual scores, routine, recommendations, and progress tools.']]
export default function HowItWorks(){const {startScan}=useScan();return <section id="how" className="redesign-band"><div className="redesign-wrap"><div className="redesign-section-head"><h2>From one photo to a routine you can follow.</h2><p>No lengthy quiz. You take the photo, SkinAI does the visual analysis, and you get a plan.</p></div><div className="redesign-steps">{steps.map(([title,text])=><article key={title}><h3>{title}</h3><p>{text}</p></article>)}</div><button className="redesign-text-action" onClick={startScan}>Take your photo →</button></div></section>}
