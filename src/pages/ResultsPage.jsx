import {useLocation,useNavigate} from 'react-router-dom';import Navbar from '../components/Navbar.jsx';import Footer from '../components/Footer.jsx';
const descriptions={acne:'Visible red-spot signal',hydration:'Brightness and color balance',glow:'Brightness consistency',texture:'Fine-detail signal',dark_circles:'Under-eye luminance',oiliness:'Specular highlight signal',smoothness:'Inverse texture variation'};
function Ring({label,value,color}){return <div className="score-ring" style={{'--score':`${value*3.6}deg`,'--ring':color}}><b>{value}</b><span>{label}</span></div>}
export default function ResultsPage(){const {state}=useLocation(),nav=useNavigate();if(!state?.scores)return <><Navbar/><main className="empty"><h1>No scan yet.</h1><button className="button" onClick={()=>nav('/scan')}>Upload a facial image</button></main></>;const s=state.scores,overall=state.overall_score;return <><Navbar/><main className="results-page"><div className="result-head"><div><div className="section-label">YOUR SCAN</div><h1>Skin report</h1><p>Scores are computed from the uploaded image's color, brightness, contrast and texture features. They are not medical diagnoses.</p></div><button className="button ghost" onClick={()=>nav('/scan')}>New scan</button></div><section className="rings"><Ring label="SKIN SCORE" value={overall} color="#9765eb"/><Ring label="HYDRATION" value={s.hydration} color="#8cc7fa"/><Ring label="GLOW" value={s.glow} color="#ef95c9"/></section><section className="metric-board">{Object.entries(descriptions).map(([key,label])=><article key={key}><div><span>{key.replace('_',' ')}</span><b>{s[key]}<small>/100</small></b></div><p>{label}</p><i style={{width:`${s[key]}%`}}/></article>)}</section><section className="result-grid"><div className="result-box"><div className="section-label">PERSONALIZED INSIGHTS</div>{state.insights.map(x=><p key={x}>• {x}</p>)}</div><div className="result-box"><div className="section-label">TRACK SKIN PROGRESS</div><p>Today’s visual score: <b>{overall}/100</b></p><div className="progress-bars">{[55,61,57,68,72,overall].map((v,i)=><i key={i} style={{height:`${v}%`}}/>)}</div><small>Add a second scan to compare image-derived change.</small></div></section><section className="routine-grid">{Object.entries(state.routine).map(([time,items])=><div className="result-box" key={time}><div className="section-label">{time} ROUTINE</div><ol>{items.map(item=><li key={item}>{item}</li>)}</ol></div>)}</section><section className="result-box"><div className="section-label">RECOMMENDED FOR YOU</div><div className="products">{state.recommendations.map(p=><button key={p.name} className="product-card" onClick={()=>alert(`${p.name}: ${p.reason}`)}><b>{p.name}</b><span>{p.reason}</span><small>{p.match}% match · click for detail</small></button>)}</div></section>{state.comparison&&<section className="result-box comparison"><div className="section-label">BEFORE / AFTER COMPARISON</div><p>Overall score change: <b>{state.comparison.overall_change>0?'+':''}{state.comparison.overall_change} points</b></p><div>{Object.entries(state.comparison.metric_changes).map(([k,v])=><span key={k}>{k}: {v>0?'+':''}{v}</span>)}</div></section>}</main><Footer/></>}
