import { Link } from 'react-router-dom';import Navbar from '../components/Navbar.jsx';import Footer from '../components/Footer.jsx';import {useAuth} from '../context/AuthContext.jsx';
export default function HistoryPage(){const {user,scans}=useAuth(),history=scans();return <><Navbar/><main className="history-page"><div className="section-label">YOUR ACCOUNT</div><h1>Scan history</h1>{!user?<div className="empty"><p>Sign in to save and revisit your scans.</p><Link className="button" to="/">Return home</Link></div>:history.length===0?<div className="empty"><p>Your completed scans will appear here in reverse chronological order.</p><Link className="button" to="/scan">Start your first scan</Link></div>:<div className="history-grid">{history.map(scan=><article key={scan.id} className="history-card">{scan.thumbnail&&<img src={scan.thumbnail} alt="Scan thumbnail"/>}<div><time>{new Date(scan.timestamp).toLocaleString()}</time><b>Skin score {scan.overall_score}/100</b><div className="history-scores">{Object.entries(scan.scores).slice(0,5).map(([name,value])=><span key={name}>{name.replace('_',' ')} <strong>{value}</strong></span>)}</div></div></article>)}</div>}</main><Footer/></>}
